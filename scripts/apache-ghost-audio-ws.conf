# WebSocket relay for Ghost Note remote audio (include in SSL vhost).
# Requires: a2enmod proxy proxy_wstunnel

ProxyPass /ghost/audio/ws ws://127.0.0.1:8767/
ProxyPassReverse /ghost/audio/ws ws://127.0.0.1:8767/
