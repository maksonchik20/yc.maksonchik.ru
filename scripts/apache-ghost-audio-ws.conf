# WebSocket relay for Ghost Note remote audio.
# Include INSIDE the *:443 VirtualHost block (before </VirtualHost>).
# Requires: a2enmod proxy proxy_wstunnel

ProxyPass /ghost/audio/ws ws://127.0.0.1:8767/ retry=0 timeout=30
ProxyPassReverse /ghost/audio/ws ws://127.0.0.1:8767/
